---
title: {{ title }}  # 自动填充你输入的文章标题
date: {{ date }}   # 自动填充创建时间
tags: {{ tags }}   # 预留标签数组，可填默认标签如 [Hexo, 技术]
categories: {{ categories || 'Linux运维'  }}  # 固定默认分类
author: {{ author || 'Judy' }}  # 有指定则用指定值，无则用默认
#cover: {{ cover || '/images/default-cover.jpg' }}  # 固定默认封面（可替换为你的默认图）
published: true
---
